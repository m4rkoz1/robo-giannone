# Copie para .env ou configure no EasyPanel > Environment
# Gere um SECRET_KEY seguro: python -c "import secrets; print(secrets.token_hex(32))"

# Obrigatório para persistência real (Postgres no EasyPanel)
DATABASE_URL=postgresql://giannone:giannone123@giannone-db:5432/giannone

# Segurança - troque em produção!
SECRET_KEY=troque_aqui_por_um_hex_aleatorio_de_64_chars

# Opcional: caminho do SQLite se não usar Postgres
# SQLITE_PATH=/app/data/giannone.db
